---
interface Props { as?: "div" | "section" | "article"; class?: string; }
const { as: Tag = "div", class: className = "" } = Astro.props;
---
<Tag class:list={["site-container", className]}><slot /></Tag>
